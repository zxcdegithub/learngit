1. mkdir ~/bin
2. set $PATH
    a. sudo gedit ~/.bashrc
    b. Add "export PATH=~/bin:$PATH" to the end of doc, save doc
    c. Execute cmd "source ~/.bashrc" to set make $PATH to effect.
3. curl http://php.webtutor.pl/en/wp-content/uploads/2011/09/repo > ~/bin/repo
4. chmod a+x ~/bin/repo
5. Execute repo to test install 
